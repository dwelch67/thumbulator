# Unit Tests for thumbulator 

These were built with gcc binaries from ARM.

# Minimal 

A simple loop, followed by a breakpoint: 

```
./thumbulator unit_tests/gcc/minimal.bin
``` 

# Minimal-svc

Exercise the SVC interface.
